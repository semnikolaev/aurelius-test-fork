credentials = {
    # Uncomment when using basic auth instead of keycloak auth
    #"atlas.credentials.username": "YOUR_BASIC_AUTH_USERNAME",
    #"atlas.credentials.password": "YOUR_BASIC_AUTH_PASSWORD",
    "keycloak.client.secret.key": "YOUR_KEYCLOAK_PUBLIC_KEY",
    "keycloak.credentials.username": "YOUR_KEYCLOAK_USERNAME",
    "keycloak.credentials.password": "YOUR_KEYCLOAK_PASSWORD"
}
